# MkDocs setup (QSeaBattle spec)

## Install
```bash
pip install -r requirements-docs.txt
```

## Run locally
From the repo root (where mkdocs.yml lives):
```bash
mkdocs serve
```
Then open the local URL shown in the terminal.

## Build static site
```bash
mkdocs build
```
Output goes to ./site by default.
