
# `{function_name}`

**Signature**: `{function_name}({full_signature})`  
**Module**: `{python.import.path}`

## Purpose
{What it computes and why it exists.}

## Arguments
| Name | Type | Shape | Description |
|---|---|---|---|
| `{arg}` | `{type}` | `{shape}` | {desc} |

## Returns
| Type | Shape | Description |
|---|---|---|
| `{type}` | `{shape}` | {desc} |

## Algorithm
1. {step}
2. {step}

## Errors
- `{ErrorType}` when …

!!! example "Usage"
    ```python
    y = {function_name}(x, ...)
    ```
